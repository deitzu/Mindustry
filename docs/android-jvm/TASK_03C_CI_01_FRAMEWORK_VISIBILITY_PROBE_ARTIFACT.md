# TASK_03C_CI_01 — Publish Android Framework Visibility Probe Artifact

## Status

COMPLETE — the existing Android framework visibility probe is now built and published by the existing Continuous Build without changing its semantics or production runtime behavior.

## Branch

`android-jvm`

## Baseline

`e61228a3fef2e8854c56990fab094f87f71c83dc`

## Result commit

`0e65ae64ba4d8876979abbec88b6411d4dca0a99`

## Objective

Publish the already-existing framework visibility probe JAR as a dedicated GitHub Actions artifact so it can be executed unchanged on the real Mojo Android-JVM runtime.

No Android framework/classloader fix was attempted.

## Scope

Changed only:

```text
.github/workflows/ci.yml
```

The existing probe implementation was reused unchanged:

```text
scripts/android-jvm/android-framework-visibility-probe.sh
```

No production launcher, SDL, GLES, Arc, renderer, framework-stub, or `android.jar` runtime changes were made.

## CI implementation

The existing `Test and build` job now runs:

```text
Build Android-JVM framework visibility probe
    -> bash scripts/android-jvm/android-framework-visibility-probe.sh

Upload Android-JVM framework visibility probe
    -> ci-artifacts/android-jvm-framework-visibility-probe
```

Published artifact name:

```text
Android-JVM-framework-visibility-probe-${github.sha}
```

The upload uses `if-no-files-found: error`, so a missing probe JAR fails the CI step rather than silently producing an empty artifact.

No second probe implementation or duplicate compilation path was added.

## CI result

Continuous Build:

- run number: `184`
- run ID: `35720970888`
- head SHA: `0e65ae64ba4d8876979abbec88b6411d4dca0a99`
- workflow: `Continuous Build`

The probe-specific CI steps completed successfully:

- Build Android-JVM framework visibility probe: PASS
- Upload Android-JVM framework visibility probe: PASS

The separate `Arc Android ARM64 native probe` job also completed successfully.

At the time of task documentation, the overall `Test and build` job was still progressing through the existing downstream Android/native build stages. This task does not treat that unrelated downstream progress as required evidence for the artifact publication itself.

## Artifact

Published artifact:

```text
Android-JVM-framework-visibility-probe-0e65ae64ba4d8876979abbec88b6411d4dca0a99
```

GitHub Actions artifact ID:

```text
10690994037
```

Artifact ZIP size:

```text
4111 bytes
```

Artifact ZIP digest reported by GitHub:

```text
sha256:a8ce92c3886e6d9db94030814ddd773ff003d9d87c552fdf885390a4d9095615
```

## Artifact inspection

The artifact was downloaded from GitHub Actions and inspected directly.

ZIP contents:

```text
Mindustry-android-jvm-framework-visibility-probe.jar
```

The JAR contains exactly:

```text
META-INF/
META-INF/MANIFEST.MF
androidjvm/probe/AndroidFrameworkVisibilityProbe.class
```

Direct inspection found:

- expected probe class present;
- expected manifest present;
- no `android/*` classes;
- no `java/*` classes;
- no `javax/*` classes;
- no Android framework stubs;
- no additional probe implementation.

Downloaded JAR SHA-256:

```text
fa3f774f55dfd49fc2eabfee4ee1d57804fe6e937bc75767c0a9b1cae7383076
```

The ZIP contained exactly the JAR generated by the existing probe script in CI.

## Evidence classification

### Confirmed by CI/build

- The existing framework probe script executes successfully in the Continuous Build.
- The expected probe JAR is produced.
- The artifact upload step succeeds.
- The artifact is published under a SHA-specific name.
- The artifact contains the generated JAR path expected by the script.

### Confirmed by artifact inspection

- The downloaded artifact contains exactly one JAR.
- The JAR contains only its manifest and the probe class.
- No Android framework implementation classes or JDK classes are bundled.
- The artifact is suitable for execution as a standalone probe JAR.

### Inference

- The published artifact can be transferred directly to the next runtime operator without rebuilding the probe.
- The SHA-specific artifact name provides a direct association with the CI source commit.

### Unknown

- Android framework class visibility on the real Mojo Android-JVM runtime.
- Whether any of the nine selected Android framework classes resolve through SYSTEM, CONTEXT, or BOOTSTRAP on the real device.
- Whether the probe behaves differently when launched independently versus inside the Mindustry JAR.

## Build/test verification

Repository-level change inspection:

- implementation commit contains only `.github/workflows/ci.yml`;
- the existing probe source was not modified;
- no production source files changed;
- no Arc revision change was made.

The workflow's probe stage invokes the existing script directly; no ad-hoc Java implementation was introduced in CI.

## Known limitations

- The real Mojo Android-JVM runtime has not executed this newly published artifact in this task.
- Overall Continuous Build downstream stages were still running when this report was recorded, so this report does not claim full workflow completion.
- Artifact publication success is not evidence that Android framework classes are visible.

## Next task

Execute the published probe JAR on the real Mojo Android-JVM runtime and capture the complete classloader-specific output for all nine Android framework classes before selecting any production framework/classloader change.

## Handoff

Status:
COMPLETE

Branch:
`android-jvm`

Baseline:
`e61228a3fef2e8854c56990fab094f87f71c83dc`

Commit:
`0e65ae64ba4d8876979abbec88b6411d4dca0a99`

Files changed:
- `.github/workflows/ci.yml`

Result:
The existing framework visibility probe is automatically built and published by the existing Continuous Build. The published artifact was downloaded and directly inspected; it contains the expected standalone probe JAR and no Android framework/JDK stubs.

CI:
Continuous Build #184 / run `35720970888`.
Probe build: PASS.
Probe artifact upload: PASS.
Arc Android ARM64 native probe job: PASS.
Overall `Test and build` job was still progressing at report time.

Build:
`scripts/android-jvm/android-framework-visibility-probe.sh` executed successfully in CI and generated the expected JAR.

Verification:
GitHub artifact ID `10690994037`; ZIP digest `sha256:a8ce92c3886e6d9db94030814ddd773ff003d9d87c552fdf885390a4d9095615`. Direct JAR inspection passed and found no bundled Android/JDK framework classes.

Artifact:
`Android-JVM-framework-visibility-probe-0e65ae64ba4d8876979abbec88b6411d4dca0a99`

Known limitations:
Physical Mojo execution remains pending. This task does not claim Android framework visibility or a production fix.

Next task:
Run the published artifact on the real Mojo Android-JVM runtime and record the nine-class, three-loader visibility results.
