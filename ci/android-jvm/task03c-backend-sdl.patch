diff --git a/backends/backend-sdl/build.gradle b/backends/backend-sdl/build.gradle
index 2deda3bb5d51501ef946d95dad2c2b8e23af29be..950a11c27af9b75cbe08b1a81bc90973d25a81ee 100644
--- a/backends/backend-sdl/build.gradle
+++ b/backends/backend-sdl/build.gradle
@@ -88,6 +88,10 @@
     def mainRoot = "build/jnigen/sources"
 
     all{
+    }
+
+    // GLEW is required by the existing desktop OpenGL targets only.
+    each({ it.os != Android }){
         cIncludes = ["$mainRoot/glew-2.2.0/src/glew.c"]
         headerDirs = ["$mainRoot/glew-2.2.0/include"]
     }
@@ -99,6 +103,32 @@
         linkerFlags = "-shared -m64".split(" ")
     }
 
+    addAndroid(AndroidABI.ABI_ARM64_V8A){
+        def sdlRoot = System.getenv("SDL2_ANDROID_ROOT")
+        if(sdlRoot == null || sdlRoot.isBlank()){
+            throw new GradleException("SDL2_ANDROID_ROOT is required for the Android SDL probe")
+        }
+
+        def sdlBuild = new File(sdlRoot, "build").absolutePath
+        headerDirs += (String[])[
+            new File(sdlRoot, "include/SDL2").absolutePath,
+            new File(sdlBuild, "include").absolutePath,
+            new File(sdlBuild, "include-config-release/SDL2").absolutePath
+        ]
+
+        libraries += (String[])[
+            "-L${sdlBuild}",
+            "-lSDL2",
+            "-llog",
+            "-landroid",
+            "-ldl",
+            "-lGLESv2",
+            "-lGLESv1_CM"
+        ]
+
+        androidApplicationMk += ["APP_STL := c++_static"]
+    }
+
     addWindows(x64, x86){
         def path = "SDL2-$sdlVersion/x86_64-w64-mingw32"
         def root = "$rootDir/backends/backend-sdl/build/jnigen/sources"
diff --git a/backends/backend-sdl/src/arc/backend/sdl/jni/SDLGL.java b/backends/backend-sdl/src/arc/backend/sdl/jni/SDLGL.java
index cc95b47ca5b46f6a1c25dd575d9d6600c4f5c813..39cc0c7f754e87d95d7f152f2b6136da0894bf09 100644
--- a/backends/backend-sdl/src/arc/backend/sdl/jni/SDLGL.java
+++ b/backends/backend-sdl/src/arc/backend/sdl/jni/SDLGL.java
@@ -9,9 +9,14 @@
 
     /*JNI
 
+    #ifdef __ANDROID__
+    #include <GLES3/gl3.h>
+    #include <GLES2/gl2ext.h>
+    #else
     #define GLEW_STATIC
 
     #include "GL/glew.h"
+    #endif
 
     //copied from ios openGL source, I have no idea what I'm doing
 
@@ -101,6 +106,10 @@
     public static native String init(); /*
         nativeClassInit(env);
 
+        #ifdef __ANDROID__
+        return NULL;
+        #else        nativeClassInit(env);
+
         GLenum glewError = glewInit();
         if(glewError != GLEW_OK){
             return env->NewStringUTF((const char*)glewGetErrorString(glewError));
@@ -112,6 +121,7 @@
         }else{
             return env->NewStringUTF("Missing framebuffer_object extension.");
         }
+        #endif
     */
 
     //endregion
@@ -298,21 +308,29 @@
     */
 
     public static native void glBindFramebuffer(int target, int framebuffer); /*
+        #ifdef __ANDROID__
+        glBindFramebuffer(target, framebuffer);
+        #else
         if(glBindFramebuffer){
             glBindFramebuffer(target, framebuffer);
             return;
         }
 
         glBindFramebufferEXT(target, framebuffer);
+        #endif
     */
 
     public static native void glBindRenderbuffer(int target, int renderbuffer); /*
+        #ifdef __ANDROID__
+        glBindRenderbuffer(target, renderbuffer);
+        #else
         if(glBindRenderbuffer){
             glBindRenderbuffer(target, renderbuffer);
             return;
         }
 
         glBindRenderbufferEXT(target, renderbuffer);
+        #endif
     */
 
     public static native void glBlendColor(float red, float green, float blue, float alpha); /*
@@ -340,11 +358,15 @@
     */
 
     public static native int glCheckFramebufferStatus(int target); /*
+        #ifdef __ANDROID__
+        return glCheckFramebufferStatus(target);
+        #else
         if(glCheckFramebufferStatus){
             return glCheckFramebufferStatus(target);
         }
 
         return glCheckFramebufferStatusEXT(target);
+        #endif
     */
 
     public static native void glCompileShader(int shader); /*
@@ -365,6 +387,10 @@
     */
 
     public static native void glDeleteFramebuffer(int framebuffer); /*
+        #ifdef __ANDROID__
+        GLuint b = framebuffer;
+        glDeleteFramebuffers(1, &b);
+        #else
         if(glDeleteFramebuffers){
             GLuint b = framebuffer;
             glDeleteFramebuffers(1, &b);
@@ -373,6 +399,7 @@
 
         GLuint b = framebuffer;
         glDeleteFramebuffersEXT(1, &b);
+        #endif
     */
 
     public static native void glDeleteProgram(int program); /*
@@ -380,7 +407,11 @@
     */
 
     public static native void glDeleteRenderbuffer(int renderbuffer); /*
+        #ifdef __ANDROID__
         GLuint b = renderbuffer;
+        glDeleteRenderbuffers(1, &b);
+        #else
+        GLuint b = renderbuffer;
 
         if(glDeleteRenderbuffers){
             glDeleteRenderbuffers(1, &b);
@@ -388,6 +419,7 @@
         }
 
         glDeleteRenderbuffersEXT(1, &b);
+        #endif
     */
 
     public static native void glDeleteShader(int shader); /*
@@ -501,12 +533,16 @@
     */
 
     public static native void glGetFramebufferAttachmentParameteriv(int target, int attachment, int pname, IntBuffer params); /*
+        #ifdef __ANDROID__
+        glGetFramebufferAttachmentParameteriv(target, attachment, pname, params);
+        #else
         if(glGetFramebufferAttachmentParameteriv){
             glGetFramebufferAttachmentParameteriv(target, attachment, pname, params);
             return;
         }
 
         glGetFramebufferAttachmentParameterivEXT(target, attachment, pname, params);
+        #endif
     */
 
     public static native void glGetProgramiv(int program, int pname, IntBuffer params); /*
@@ -521,12 +557,16 @@
     */
 
     public static native void glGetRenderbufferParameteriv(int target, int pname, IntBuffer params); /*
+        #ifdef __ANDROID__
+        glGetRenderbufferParameteriv(target, pname, params);
+        #else
         if(glGetRenderbufferParameteriv){
             glGetRenderbufferParameteriv(target, pname, params);
             return;
         }
 
         glGetRenderbufferParameterivEXT(target, pname, params);
+        #endif
     */
 
     public static native void glGetShaderiv(int shader, int pname, IntBuffer params); /*
@@ -581,11 +621,15 @@
     */
 
     public static native boolean glIsFramebuffer(int framebuffer); /*
+        #ifdef __ANDROID__
+        return glIsFramebuffer(framebuffer);
+        #else
         if(glIsFramebuffer){
             return glIsFramebuffer(framebuffer);
         }
 
         return glIsFramebufferEXT(framebuffer);
+        #endif
     */
 
     public static native boolean glIsProgram(int program); /*
@@ -593,11 +637,15 @@
     */
 
     public static native boolean glIsRenderbuffer(int renderbuffer); /*
+        #ifdef __ANDROID__
+        return glIsRenderbuffer(renderbuffer);
+        #else
         if(glIsRenderbuffer){
             return glIsRenderbuffer(renderbuffer);
         }
 
         return glIsRenderbufferEXT(renderbuffer);
+        #endif
     */
 
     public static native boolean glIsShader(int shader); /*
@@ -617,12 +665,16 @@
     */
 
     public static native void glRenderbufferStorage(int target, int internalformat, int width, int height); /*
+        #ifdef __ANDROID__
+        glRenderbufferStorage(target, internalformat, width, height);
+        #else
         if(glRenderbufferStorage){
             glRenderbufferStorage(target, internalformat, width, height);
             return;
         }
 
         glRenderbufferStorageEXT(target, internalformat, width, height);
+        #endif
     */
 
     public static native void glSampleCoverage(float value, boolean invert); /*
@@ -1150,7 +1202,11 @@
     */
 
     public static native void glProgramParameteri(int program, int pname, int value); /*
+        #ifdef __ANDROID__
+        env->ThrowNew(UOEClass, "glProgramParameteri is unsupported on Android GLES.");
+        #else
         glProgramParameteri(program, pname, value);
+        #endif
     */
 
     public static native void glInvalidateFramebuffer(int target, int numAttachments, IntBuffer attachments); /* 
